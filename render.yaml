services:
  - type: web
    name: pdf-translator-ai
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: singapore
    plan: free
    branch: main
    envVars:
      - key: MOCK_TRANSLATION
        value: "true"  # ✅ Keep mock mode for now
      - key: PORT
        value: "8000"
      # Add OPENAI_API_KEY later when you're ready
      # - key: OPENAI_API_KEY
      #   sync: false  # Will set this manually in Render dashboard
```